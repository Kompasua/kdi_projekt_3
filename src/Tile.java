
/**
 * A single tile in a Level
 * 
 * @author (your name here)
 * @version (version number or date here)
 */
public enum Tile
{
    /** A wall cannot be passed. */
    WALL, 
    /** Ghosts and pakman can walk through passages. */
    PASSAGE,
    /** A passage where a pill is lying */
    PILL
}
